[project]
name = "tg_notifier"
version = "0.1.0"
description = "отправка уведомлений в телеграм без зависимостей"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "setuptools>=80.9.0",
]
