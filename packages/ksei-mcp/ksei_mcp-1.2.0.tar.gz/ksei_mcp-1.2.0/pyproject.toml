[project]
name = "ksei-mcp"
version = "1.2.0"
description = "MCP server for KSEI API access"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Ichsanul Amal", email = "muhammad.ichsanul19@gmail.com"}]
dependencies = [
    "mcp>=0.9.0",
    "requests",
    "PyJWT",
    "fake-useragent",
    "python-dotenv",
    "aiohttp>=3.12.14",
]

[project.urls]
"Homepage" = "https://github.com/ichsanulamal/ksei-mcp"

[project.scripts]
ksei-mcp = "ksei_mcp.main:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
