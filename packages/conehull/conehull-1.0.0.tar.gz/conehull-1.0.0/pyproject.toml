[project]
name = "conehull"
version = "1.0.0"
authors = [
  { name="Tryggvi Kalman Jónsson", email="tryggvikalman@proton.me" },
]
description = " This package uses the quickhull algorithm to find the convex hull of a point set, but with the added option of restricting the set of halfplanes we intersect over to only those with normals lying in a given convex cone."
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "GPL-3.0"
license-files = ["LICEN[CS]E*"]

[project.urls]
Homepage = "https://github.com/Kalmander/conehull"
Issues = "https://github.com/Kalmander/conehull/issues"
