[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "dayforcepy"
version = "1.0.0"
description = "Connector for Dayforce HCM API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["pandas>=2.3.0", "requests>=2.32.4"]

[dependency-groups]
dev = ["pre-commit>=4.2.0", "ruff>=0.11.13"]
