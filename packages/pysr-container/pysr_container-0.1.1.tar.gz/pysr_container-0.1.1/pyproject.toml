[tool.poetry]
name = "pysr-container"
version = "0.1.1"
description = "Реализация контейнера внедрения зависимостей pysr-abc-container 1.0"
authors = ["Alexey Volkov <webwizardry@hotmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pysr-abc-container = "^0.1"

[tool.poetry.group.dev.dependencies]
pytest = "^8.4.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
