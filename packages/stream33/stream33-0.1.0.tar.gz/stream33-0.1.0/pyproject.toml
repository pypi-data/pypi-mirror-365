[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stream33"
version = "0.1.0"
description = "A sample Python package"
authors = [{ name = "Narendra", email = "Nari@stream33.com" }]
readme = "README.md"
requires-python = ">=3.0"
license = { text = "MIT" }