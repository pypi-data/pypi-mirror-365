[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "genai-colosseum"
version = "0.1.0"
description = "A CLI tool to generate GenAI project templates"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [
  { name="Your Name", email="your@email.com" }
]
dependencies = [
  "click"
]

[project.scripts]
genai-colosseum = "genai_colosseum.main:main"
