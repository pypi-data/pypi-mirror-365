[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "genai-colosseum"
version = "0.1.2"
description = "CLI tool to generate GenAI project templates"
readme = "README.md"
requires-python = ">=3.7"
authors = [
  { name="Your Name", email="your@email.com" }
]
license = {text = "MIT"}
dependencies = ["click"]

[project.scripts]
genai-colosseum = "genai_colosseum.main:main"
