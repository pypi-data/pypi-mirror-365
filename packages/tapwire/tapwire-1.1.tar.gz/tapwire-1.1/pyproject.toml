[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tapwire"
version = "1.1"
description = "A simple program for stealing variables from other programs. think of it like config files but python."
readme = "README.md"
authors = [{ name = "StrawberryMilcRivers", email = "chocomilccy@gmail.com" }]
license = "MIT"
license-files = ["LICENSE*"]
requires-python = ">=3.8"
dependencies = []
