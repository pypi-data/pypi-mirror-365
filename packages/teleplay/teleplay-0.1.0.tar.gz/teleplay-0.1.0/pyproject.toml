[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta:__legacy__"

[project]
name = "teleplay"
version = "0.1.0"
authors = [
    {name = "Douglas Luman", email = "dluman@allegheny.edu"}
]
readme = "README.md"
description = "Python CLI script testing with some support for arbitrary command line flags."

[project.scripts]
teleplay = "teleplay:main.main"
