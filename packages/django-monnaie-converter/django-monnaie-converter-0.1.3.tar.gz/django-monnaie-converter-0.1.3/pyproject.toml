[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "monnaie-converter"
version = "0.1.3"
description = "Un package Django pour convertir les monnaies avec un formulaire simple"
readme = "README.md"
requires-python = ">=3.7"
authors = [{ name = "Nicosidick", email = "abou210traore@email.com" }]
license = { text = "MIT" }
dependencies = ["Django>=3.2"]

[project.urls]
"Homepage" = "https://pypi.org/project/monnaie-converter/"
