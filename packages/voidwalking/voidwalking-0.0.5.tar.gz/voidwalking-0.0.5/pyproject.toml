[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "voidwalking"
version = "0.0.5"
description = "Empty space finder for point pattern data. Random walks of point repulsive voids, increasing in size, through space."
authors = [
    { name = "Jack Peyton", email = "jpeyton01@qub.ac.uk" }
]
readme = "README.md"
license = { file = "LICENCE" }
requires-python = ">=3.8"
dependencies = [

]

[tool.setuptools.packages.find]
where = ["."]
