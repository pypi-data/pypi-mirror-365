[project]
name = "fromless"
version = "0.0.2"
authors = [
  { name="Christopher Neugebauer", email="_@chrisjrn.com" },
]
description = "A base class that prevents subclasses from being instantiated from an unqualified import."
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICENCE"]

