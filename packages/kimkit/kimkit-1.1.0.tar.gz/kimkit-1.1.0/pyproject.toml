[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kimkit"
authors = [{"name"="Claire Waters", "email"="bwaters@umn.edu"}]
readme = "README.md"
license = "Apache-2.0 WITH LLVM-exception"
license-files = ["LICENSE"]
requires-python = ">=3.9"
dynamic = ["version"]
dependencies=["pytz", "kim_edn", "packaging", "pygments", "pymongo", "numpy"]