[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "codebrix"
version = "0.3.0"
description = "Nur lokal verwendetes Beispielprojekt"
authors = [{ name = "Lars Erler" }]
dependencies = []