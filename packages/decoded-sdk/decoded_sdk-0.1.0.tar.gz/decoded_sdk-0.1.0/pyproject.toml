[project]
name = "decoded-sdk"
version = "0.1.0"
description = ""
authors = [
    {name = "CIOI",email = "rhkr9693@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "typesense (>=1.1.1,<2.0.0)"
]

[tool.poetry]
packages = [{include = "decoded_sdk", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
