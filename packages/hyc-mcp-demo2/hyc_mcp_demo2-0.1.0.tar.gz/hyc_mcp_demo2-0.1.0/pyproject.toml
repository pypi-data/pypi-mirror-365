[project]
name = "hyc-mcp-demo2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.6"
dependencies = [
    "mcp[cli]>=1.12.2",
]

[project.scripts]
hyc-mcp-demo2 = "hyc_mcp_demo2:main"

[build-system]
requires = ["uv_build>=0.8.2,<0.9.0"]
build-backend = "uv_build"
