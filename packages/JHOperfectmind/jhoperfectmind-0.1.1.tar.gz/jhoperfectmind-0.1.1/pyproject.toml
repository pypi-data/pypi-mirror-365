[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "JHOperfectmind"
version = "0.1.1"

[tool.setuptools]
packages = ["common","common.helpers"] 