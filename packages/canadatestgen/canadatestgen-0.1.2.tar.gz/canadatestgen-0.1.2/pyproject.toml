[project]
name = "canadatestgen"
version = "0.1.2"
description = "Generate fake Canadian-style test data with a simple CLI or Python module."
authors = [{ name = "Your Name", email = "you@example.com" }]
license = "MIT"
readme = "README.md"
requires-python = ">=3.7"
dependencies = []

[project.scripts]
canadatestgen = "canadatestgen.cli:main"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
