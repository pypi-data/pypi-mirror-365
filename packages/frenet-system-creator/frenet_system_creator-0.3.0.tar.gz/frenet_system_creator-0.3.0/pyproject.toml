[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "frenet-system-creator"
version = "0.3.0"
authors = [{name = "Delvin Yang", email = "jyang512@connect.hkust-gz.edu.cn"}]
description = "Utilities for converting between Cartesian and Frenet frames"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
dependencies = [
    "numpy",
    "scipy"
]

[project.urls]
Homepage = "https://github.com/DelvinYang/"
