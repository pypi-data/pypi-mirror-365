[build-system]
requires = ["setuptools>=69.3"]
build-backend = "setuptools.build_meta"

[project]
name = "form-util"
version = "0.5.6"
description = "Une app Django avec un formulaire de bienvenue simple"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["django>=4.2"]
authors = [
  {name = "prince01", email = "axelvianney49@gmail.com"},
]
license = {text = "MIT"}

[project.urls]
Homepage = "https://github.com/pr1nceaxel/django-welcome-form"

[project.scripts]
form_util = "form_util.__main__:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

