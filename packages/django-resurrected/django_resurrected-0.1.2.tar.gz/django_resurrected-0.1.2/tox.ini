[tox]
requires = tox>=4.2
env_list = py{313,312,311,310,39}

[testenv]
runner = uv-venv-lock-runner
set_env = PYTHONDEVMODE=1
commands =
    coverage run -m pytest {posargs:tests}
dependency_groups = dev