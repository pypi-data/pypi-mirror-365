[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "torch-linode"
version = "0.1.0"
authors = [
  { name="Wu-Chenyang", email="wuchenyang@stu.pku.edu.cn" },
]
description = "A PyTorch-based library for batch solving homogeneous linear matrix ODEs with Magnus integrators."
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Physics",
    "Topic :: Scientific/Engineering :: Mathematics",
]
dependencies = [
    "torch>=1.13",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "matplotlib",
]

[project.urls]
"Homepage" = "https://github.com/Wu-Chenyang/torch_linode"
"Bug Tracker" = "https://github.com/Wu-Chenyang/torch_linode/issues"
